all:
  vars:
    ansible_user: demo
    ansible_ssh_private_key_file: /root/.ssh/local_key

  children:
    master:
      hosts:
        srv-master:
          ansible_host:  srv-master.mshome.net

    workers:
      hosts:
        srv-worker:
          ansible_host:  srv-worker.mshome.net